"""Unit tests for BackpackRequestBuilder utility functions."""

# Note: Symbol normalization is now handled by the Symbol object system itself
# These tests have been removed as BackpackCommonMappers.normalize_symbol no longer exists
# Symbol objects handle their own value normalization through the symbol factory
